{
  "name": "promptpro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "turbo build && mkdir -p dist/app && mkdir -p dist/docs && cp app/dist/* dist/app && cp docs/dist/* dist/docs/",
    "dev": "turbo dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.8.5"
  },
  "workspaces": [
    "*"
  ]
}
